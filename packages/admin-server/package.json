{
  "name": "@jsango/admin-server",
  "version": "1.0.0",
  "description": "Admin HTTP API server — CRUD service layer, REST endpoints, and framework integration for jsango Admin",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@jsango/core": "workspace:*",
    "@jsango/http": "workspace:*",
    "@jsango/router": "workspace:*",
    "@jsango/auth": "workspace:*",
    "@jsango/admin-core": "workspace:*",
    "@jsango/admin-auth": "workspace:*",
    "@jsango/admin-audit": "workspace:*"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/flutterbuddy1/jsango.git",
    "directory": "packages/admin-server"
  },
  "publishConfig": {
    "access": "public"
  }
}
