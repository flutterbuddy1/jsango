{
  "name": "@jsango/observability",
  "version": "1.0.0",
  "description": "Production-grade observability foundation — structured logging, metrics, tracing, health checks, and diagnostics for jsango",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@jsango/core": "workspace:*",
    "@jsango/http": "workspace:*",
    "@jsango/router": "workspace:*"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jsango/jsango.git",
    "directory": "packages/observability"
  },
  "publishConfig": {
    "access": "public"
  }
}
