# Integration & End-to-End Tests

This directory will contain cross-package integration tests and end-to-end framework test suites.
Unit tests reside colocated inside each package under `packages/*/tests/` or `packages/*/src/**/*.test.ts`.
